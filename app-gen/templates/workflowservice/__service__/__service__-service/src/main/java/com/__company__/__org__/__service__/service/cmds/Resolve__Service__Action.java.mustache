package com.{{company}}.{{org}}.{{service}}.service.cmds;

import org.chenile.stm.STMInternalTransitionInvoker;
import org.chenile.stm.State;
import org.chenile.stm.action.STMTransitionAction;
import org.chenile.stm.model.Transition;

import {{chenilePackage}}.workflow.param.MinimalPayload;
import com.{{company}}.{{org}}.{{service}}.model.{{Service}};

public class Resolve{{Service}}Action implements STMTransitionAction<{{Service}}>{

	@Override
	public void doTransition({{Service}} {{service}}, Object transitionParam, State startState, String eventId,
			State endState, STMInternalTransitionInvoker<?> stm, Transition transition) throws Exception {
		MinimalPayload payload = (MinimalPayload) transitionParam;
		{{service}}.resolveComment = payload.getComment();
	}

}
